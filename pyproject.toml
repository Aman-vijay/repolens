[project]
name = "repolens"
version = "0.1.0"
description = "RepoLens — understand and plan changes in unfamiliar codebases"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["apps/api", "workers", "packages/db"]
